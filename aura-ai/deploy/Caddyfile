# Caddy in front of the Aura server. Gets and renews a Let's Encrypt certificate on its own,
# which is what makes the microphone work: browsers only hand over mic and camera on a secure
# origin, and that was the whole reason the LAN setup needed a hand-rolled self-signed cert.
#
#   sudo cp Caddyfile /etc/caddy/Caddyfile
#   sudo systemctl reload caddy
#
# The hostname must already resolve to this server's IP before Caddy starts, or the
# certificate request fails — and a failed request counts against Let's Encrypt's rate limit.
#
# To go live before any DNS record exists, use sslip.io instead: it resolves any hostname of
# the form <ip>.sslip.io straight to that IP, with nothing to configure. Swap the line below
# for your server's address and Caddy gets a genuine, browser-trusted certificate for it:
#
#     149.28.167.17.sslip.io {
#
# Moving to the real subdomain later is just this line plus the A record — no app changes,
# though the phone will need the new URL entered once.

aura.cactusdesign.au {
	# WebSockets need no special handling in Caddy v2 — upgrades are proxied as they come.
	reverse_proxy 127.0.0.1:8000

	encode gzip

	header {
		# The page is the same origin as its brain, so nothing needs to embed it.
		X-Frame-Options DENY
		X-Content-Type-Options nosniff
		Referrer-Policy no-referrer
	}

	# config.js carries the mode the page runs in and must never be cached between deploys.
	@config path /config.js
	header @config Cache-Control "no-store"

	log {
		output file /var/log/caddy/aura.log
	}
}
